#include "../push_swap.h"
/* 
int cost(void)
{
	int i;
	int j;
	int tmp;
	int tmp_2;

	i = 0;
	tmp = 0;
	tmp_2 = 0;
	while (i < stack_b()->length)
	{
		j = i + 1;
		while(j < stack_b()->length)
		{
			if(j < stack_b()->length / 2)
				tmp_2 = j;
			else if (j >= stack_b()->length / 2)
				tmp_2 = j - stack_b()->length;
			
		}
	}
}

int cost_a(int num)
{
	int i;

	i = 0;
	if (compare_cases(num) >= stack_a()->length / 2)
		return ()
} */